---
/**
 * Home `/` — repositioned homepage from spec/content.md §1 (AI-agent-first positioning),
 * with Batch-1 facts woven in: the moat (cleared audits), the reconciled care story,
 * scale-driven drug pricing, and the "3 years in market" proof. Static render; the Phase 2
 * motion engine layers in the hero timeline, pinned flow, and count-up tweens.
 */
import PageLayout from '../layouts/PageLayout.astro';
import Section from '../components/layout/Section.astro';
import SectionHeader from '../components/primitives/SectionHeader.astro';
import { Icon } from 'astro-icon/components';
import Button from '../components/primitives/Button.astro';
import Card from '../components/primitives/Card.astro';
import StatCard from '../components/primitives/StatCard.astro';
import Badge from '../components/primitives/Badge.astro';
import FAQ from '../components/primitives/FAQ.astro';
import HeroArt from '../components/marketing/HeroArt.astro';
import Heritage from '../components/marketing/Heritage.astro';
import { organizationJsonLd, softwareApplicationJsonLd, faqJsonLd } from '../lib/seo';
import { STATS, RX_ODOMETER, PROOF_LINE } from '../lib/stats';
import { DOCS_URL } from '../lib/nav';

/** StatCard props from a registry entry — no literal stats in this file (G1). */
const stat = (key: keyof typeof STATS) => ({
  value: STATS[key].value,
  label: STATS[key].label,
  caption: STATS[key].caption,
});

const modules = [
  {
    icon: 'clipboard-list',
    title: 'Intake',
    body: 'Agent- and API-first patient intake with identity verification built in. Assemble condition-aware questionnaires from tested components and prompts; your agents run them.',
    href: '/platform/intake',
  },
  {
    icon: 'stethoscope',
    title: 'Visits & E-Prescribing',
    body: 'Synchronous and async telemedicine visits with certified e-prescribing, routed by a real-time, multi-state provider engine.',
    href: '/platform/visits',
  },
  {
    icon: 'users',
    title: 'Patient Management',
    body: 'One longitudinal record, secure messaging, and care coordination across every module.',
    href: '/platform/patients',
  },
  {
    icon: 'pill',
    title: 'Pharmacy & Fulfillment',
    body: 'A nationwide 503A/503B network with volume pricing passed straight through to you — zero markup — shipping to all 50 states.',
    href: '/platform/pharmacy-fulfillment',
  },
  {
    icon: 'credit-card',
    title: 'Payments',
    body: 'Telehealth merchant accounts, subscriptions, and refunds — callable from your stack.',
    href: '/platform/payments',
  },
  {
    icon: 'trending-up',
    title: 'Analytics & Growth',
    body: 'Cohorts, retention, order economics, conversion, and BI as data and endpoints — not locked dashboards.',
    href: '/platform/analytics',
  },
];

const audits = ['HIPAA', 'Pharmacy-board', 'Payment-processor'];

// Wearable & device ecosystem (folded from WLMD) — names as text chips; actual brand
// logos land once brand-use permission is confirmed, like customer logos.
const devices = [
  'Apple Health',
  'Fitbit',
  'Garmin',
  'Dexcom',
  'Withings',
  'Google Fit',
  'Polar',
  'Samsung Health',
];

// Initial server-rendered value for the live Rx ticker; the hero <script> imports the
// same registry anchor, so the two can never drift.
const RX_ANCHOR = RX_ODOMETER.anchorCount;

const programs = [
  {
    icon: 'activity',
    title: 'Weight loss',
    body: 'The category driving telehealth demand — GLP-1 and metabolic programs, end to end. Our flagship.',
  },
  {
    icon: 'user',
    title: "Men's health",
    body: 'Sexual health, hormones, hair, and wellness under one branded program.',
  },
  {
    icon: 'heart-pulse',
    title: "Women's health",
    body: 'Hormones, wellness, and longevity care, fully branded.',
  },
  {
    icon: 'dumbbell',
    title: 'TRT & HRT',
    body: 'Testosterone and hormone therapy with labs and titration built in.',
  },
  {
    icon: 'flask-conical',
    title: 'Peptides',
    body: 'Recovery and longevity protocols on compliant, compounded supply.',
  },
  {
    icon: 'microscope',
    title: 'Labs & diagnostics',
    body: 'Baseline and monitoring panels, ordered and resulted inside the journey.',
  },
];

// Ownership / sovereignty — the brand, patients, and data are the customer's, not ours.
const ownership = [
  {
    icon: 'tag',
    title: 'Your brand',
    body: 'Every patient touchpoint — intake, visits, billing, refills — runs under your name. Patients see your brand, never ours.',
  },
  {
    icon: 'users',
    title: 'Your patients',
    body: 'The customer relationship is yours to keep. We run the back end; you own the front.',
  },
  {
    icon: 'database',
    title: 'Your data',
    body: 'Clear data ownership and a documented chain of custody — exportable on demand. Your data stays yours.',
  },
];

const homeFaqs = [
  {
    q: 'Do I own my brand, patients, and data?',
    a: 'Yes — always. The brand, the patients, and the data are yours. Purple Label runs the back end; the customer relationship and the data stay with you.',
  },
  {
    q: 'Am I the doctor?',
    a: 'No. A licensed provider network, credentialed in all 50 states, handles clinical care and prescribing. You own the brand; licensed providers handle the medicine.',
  },
  {
    q: 'Is a doctor still involved if AI is doing the work?',
    a: 'Yes. Agents and AI handle the repeatable work, but a licensed, board-certified clinician reviews every clinical decision, prescription, and lab result.',
  },
  {
    q: 'What medical programs are supported?',
    a: "Weight loss (GLP-1), men's and women's health, TRT, HRT, ED, hair & skin, peptides, and integrated labs — plus anything else legal and compliant your agents compose from the same modules.",
  },
  {
    q: 'How does LegitScript certification work?',
    a: 'As a LegitScript Enterprise Partner, your brand inherits pre-certified infrastructure and a fast-track process across pharmacies, providers, and websites — so you can advertise and process payments from day one.',
  },
  {
    q: 'Who handles medication and fulfillment?',
    a: 'A nationwide 503A/503B pharmacy network fills and ships to all 50 states — at pass-through pricing, zero markup. Your cost is our cost.',
  },
  {
    q: 'Do I build it myself, or is it run for me?',
    a: 'Either. Build on the platform with your own agents and developers, run a dedicated enterprise deployment, or have our team build and operate it for you on the Managed plan.',
  },
  {
    q: 'What does it take to launch?',
    a: 'The modules are already in production — intake, visits, e-prescribing, pharmacy, payments, analytics, and compliance. Your agents compose and operate a branded program on infrastructure that has already cleared HIPAA, pharmacy-board, and payment-processor audits.',
  },
  {
    q: 'How much does it cost to launch?',
    a: 'A fraction of building your own infrastructure, with flexible models across the tiers — including Managed, where we work on revenue share and only do well when you do. We scope the specifics on your consult.',
  },
  {
    q: 'Can my brand offer lab testing and wearable programs?',
    a: `Yes — diagnostics with at-home and in-person collection, plus ${STATS.deviceIntegrations.value} wearable and device integrations, all running inside your branded platform.`,
  },
  {
    q: 'Can you make a custom product or a custom-dosed formulation?',
    a: 'Yes — custom compounded formulations and branded packaging with our 503A/503B partners. Anything legal and compliant, built around your vision.',
  },
];
---

<PageLayout
  title="Purple Label MD — launch and own a branded telehealth business"
  description="Launch and own a branded telehealth business on proven, audit-cleared infrastructure — providers, pharmacy at pass-through pricing, payments, and compliance. Build it with your own agents and API, or have our team run it for you, done-for-you on revenue share."
  path="/"
  jsonLd={[organizationJsonLd(), softwareApplicationJsonLd(), faqJsonLd(homeFaqs)]}
>
  {
    /* 1 — Hero: eyebrow across the top, headline left / support + CTAs right
      (top-aligned), and the saga animation blending up into the same section —
      one continuous composition that walks the eye from headline to diagram. */
  }
  <Section size="lg">
    <p
      class="flex flex-wrap items-baseline gap-x-2 text-xs uppercase tracking-eyebrow text-ink-400"
    >
      <span>Rx filled by our operators' platforms to date</span>
      <span
        id="rx-counter"
        class="font-mono text-sm tabular-nums tracking-normal text-purple-600 normal-case"
        >{RX_ANCHOR.toLocaleString('en-US')}</span
      >
    </p>

    <div class="mt-5 grid gap-10 lg:grid-cols-12 lg:gap-14">
      <div class="lg:col-span-7">
        {
          /* The explicit {' '} matters: Astro collapses the newline before the span,
            which shipped as "brandedtelehealth" on the live hero. */
        }
        <h1 class="font-display text-display text-ink-900">
          Launch and own a branded{' '}
          <span class="text-purple-600">telehealth business.</span>
        </h1>
      </div>

      <div class="lg:col-span-5 lg:pt-3">
        <p class="max-w-prose text-body-lg text-ink-500">
          Providers, pharmacy, payments, and compliance — from the team behind{' '}
          {STATS.brandsLaunched.value} branded telehealth launches, rebuilt API-first for the agent era.
          Medication at pass-through pricing, zero markup. Build it with your agents, or have our team
          run it for you.
        </p>
        {
          /* The POST button IS the demo: it fires the saga below and glides the page
            down in sync (see HeroArt). "Start building" (docs) lives on in the
            two-ways section; the journey button is the hero's developer hook now. */
        }
        <div class="mt-8 flex flex-wrap items-center gap-4">
          <Button variant="primary" href="/contact">Book a consult</Button>
          <Button variant="secondary" type="button" id="run-journey">
            <Icon name="lucide:play" class="icon size-4 text-purple-600" />
            Patient journey
          </Button>
          <Button variant="secondary" href={DOCS_URL}>
            <Icon name="lucide:code" class="icon size-4 text-purple-600" />
            Read the docs
          </Button>
        </div>
        {
          /* No free-floating separator dots — they strand at line ends when this wraps
            on phones. Each item carries its own icon and wraps as a unit. */
        }
        <ul class="mt-8 flex flex-wrap items-center gap-x-4 gap-y-2 text-sm text-ink-400">
          <li class="inline-flex items-center gap-1.5">
            <Icon name="lucide:shield-check" class="icon size-4 shrink-0 text-success" />
            LegitScript Enterprise Partner
          </li>
          <li class="inline-flex items-center gap-1.5">
            <Icon name="lucide:check" class="icon size-4 shrink-0 text-success" />
            HIPAA-compliant
          </li>
          <li class="inline-flex items-center gap-1.5">
            <Icon name="lucide:check" class="icon size-4 shrink-0 text-success" />
            50-state network
          </li>
        </ul>
      </div>
    </div>

    {
      /* The saga — same section, no seam; it rises toward the headline block so the
        POST chip sits just under the copy and the pipeline carries the eye down. */
    }
    <div class="mx-auto -mb-6 mt-0 max-w-5xl md:-mb-14 md:-mt-4">
      <HeroArt />
    </div>
  </Section>

  {/* 1.5 — Scale strip, right under the hero (per Gregg 2026-07-23) */}
  <Section size="sm" surface="surface" bordered>
    <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-5">
      <StatCard {...stat('rxMonthly')} />
      <StatCard {...stat('providerNetwork')} />
      <StatCard {...stat('deviceIntegrations')} />
      <StatCard {...stat('legitscriptCerts')} />
      <StatCard {...stat('ordersWeekly')} />
    </div>
  </Section>

  {/* 2 — Proof bar */}
  <Section size="sm" surface="surface" bordered>
    <p class="mx-auto max-w-prose text-center text-body text-ink-500">
      From the operators of a national-scale telehealth machine — {STATS.brandsLaunched.value}
      branded launches — rebuilt API-first as Purple Label.
    </p>
    <p class="mt-6 text-center text-xs uppercase tracking-eyebrow text-ink-400">
      Trusted by founders like Trucker's Body Shop — more logos as permissions land
    </p>
  </Section>

  {/* 3 — The thesis */}
  <Section>
    <SectionHeader
      eyebrow="Why now"
      title="One platform, built around your business."
      lead="For a decade, launching telehealth meant buying a no-code builder, a separate EMR, a prescribing tool, a pharmacy, and a payment processor — then wiring them together and running it all through a dozen dashboards. Purple Label gives you the proven layer underneath instead: a modular platform you can run two ways — build on it with your own team and agents, or have us build and operate it for you."
    />
  </Section>

  {/* 3.5 — Heritage × next-gen (the frame that separates proof from product) */}
  <Section surface="surface" bordered>
    <Heritage />
  </Section>

  {/* 4 — The moat */}
  <Section surface="sunken" bordered>
    <SectionHeader
      eyebrow="The moat"
      title="Production infrastructure that's already passed the audits."
      lead="Anyone can generate a telehealth site. Almost no one can pass a state pharmacy-board inspection."
    />
    <p class="mt-6 max-w-prose text-body-lg text-ink-500">
      Competitors spinning up no-code or AI-generated sites hit a wall the moment they try to ship a
      prescription across state lines, refund a patient, or pass a state pharmacy-board inspection.
      We've already paid those costs — you launch on infrastructure that has already {PROOF_LINE},
      on a HIPAA-compliant data layer with end-to-end encryption.
    </p>
    <div class="mt-8 flex flex-wrap items-center gap-3">
      {
        audits.map((a) => (
          <Badge tone="success" icon="badge-check">
            {a} — cleared
          </Badge>
        ))
      }
    </div>
    <p class="mt-8 max-w-prose text-body-lg text-ink-700">
      Underneath, a regulated multi-party pipeline is collapsed into a single durable function —
      engineered so it won't double-charge, double-prescribe, or strand a patient mid-flow. Your
      agents drive it with a few API and MCP calls.
    </p>
    <p class="mt-8 max-w-prose font-display text-h3 text-ink-900">
      What took telehealth unicorns years and acquisitions, you get on day one.
    </p>
    <div class="mt-6">
      <Button variant="link-arrow" href="/architecture">See the architecture</Button>
    </div>
  </Section>

  {/* 5 — Two ways to launch */}
  <Section>
    <SectionHeader
      eyebrow="Two ways to launch"
      title="Build it yourself, or have us run it."
      lead="Same proven platform, same cleared reviews, same pass-through drug pricing — you choose how hands-on you want to be."
    />
    <div class="mt-12 grid gap-6 md:grid-cols-2">
      <Card icon="code" title="Build it with agents">
        Point your team and your AI agents at API tokens, agent-ready docs, branded entry links, and
        webhooks with signed deliveries — and assemble a branded telehealth business in code. Full
        control, no glue, no console to outgrow.
        <span class="mt-6 block">
          <Button variant="link-arrow" href={DOCS_URL}>Start building</Button>
        </span>
      </Card>
      <Card icon="headset" title="We run it for you">
        Prefer done-for-you? Our team builds, staffs, and operates your branded store — providers,
        pharmacy, compliance, and marketing handled — typically live in about 30 days, on revenue
        share.
        <span class="mt-6 block">
          <Button variant="link-arrow" href="/managed">Explore Managed</Button>
        </span>
      </Card>
    </div>
  </Section>

  {/* 6 — The modular toolkit */}
  <Section surface="surface" bordered>
    <SectionHeader
      eyebrow="The modular toolkit"
      title="Best-of-breed modules, battle-tested at scale."
      lead="Adopt the modules you need and call them from your stack. One source of truth for patients, orders, and clinical data."
    />
    <div class="mt-12 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
      {
        modules.map((m) => (
          <Card icon={m.icon} title={m.title} href={m.href}>
            {m.body}
          </Card>
        ))
      }
    </div>
    <div class="mt-6 grid gap-6 md:grid-cols-2">
      <Card icon="watch" title="Wearables & Monitoring" href="/platform/wearables">
        {STATS.deviceIntegrations.value} wearable and device integrations — rings, watches, CGMs, scales,
        and more — stream patient data into the same longitudinal record your programs act on.
      </Card>
      <Card icon="shield-check" title="Security & Compliance">
        A HIPAA-compliant data layer with end-to-end encryption and access governance under every
        module — behind cleared HIPAA, state pharmacy-board, and payment-processor reviews.
      </Card>
    </div>
    <ul class="mt-8 flex flex-wrap items-center gap-x-3 gap-y-2" aria-label="Device integrations">
      <li class="text-xs uppercase tracking-eyebrow text-ink-400">Syncs with</li>
      {
        devices.map((d) => (
          <li class="rounded-md border border-line bg-paper px-3 py-1.5 text-sm font-medium text-ink-500">
            {d}
          </li>
        ))
      }
      <li class="text-sm text-ink-400">…and {STATS.deviceIntegrations.value} in all</li>
    </ul>
    <p class="mt-4 text-xs text-ink-300">
      All trademarks are the property of their respective owners. Listing indicates data
      compatibility, not endorsement.
    </p>
  </Section>

  {/* 6.5 — Rx catalog */}
  <Section>
    <SectionHeader
      eyebrow="The Rx catalog"
      title="Any compliant program, launched under your brand."
      lead="The same modules compose into complete, branded care programs — already running on the platform at scale, on medication passed straight through at our cost — zero markup. Build one with your agents, or have us run it. Launch one, or a full menu."
    />
    <div class="mt-12 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
      {
        programs.map((p) => (
          <Card icon={p.icon} title={p.title}>
            {p.body}
          </Card>
        ))
      }
    </div>
    <div class="mt-8">
      <Button variant="link-arrow" href="/programs">See the full Rx catalog</Button>
    </div>
  </Section>

  {/* 7 — Why build on Purple Label (the 3 differentiators) */}
  <Section>
    <SectionHeader
      eyebrow="Why build on Purple Label"
      title="You inherit infrastructure that already scaled."
      lead="How you build is up to you. These are why teams choose us either way."
    />
    <div class="mt-12 grid gap-6 md:grid-cols-3">
      <Card icon="server" title="Proven scale, top-tier security">
        Built by the operators of a national telehealth machine — {STATS.brandsLaunched.value}
        branded launches, {STATS.rxMonthly.value} prescriptions a month across the partner pharmacy network,
        {STATS.ordersWeekly.value} orders a week — and {PROOF_LINE}.
      </Card>
      <Card icon="circle-dollar-sign" title="Among the lowest medication costs in telehealth">
        We pass our huge-volume Rx discounts straight through with zero markup — our cost is your
        cost. Keep the margin, or pass the savings to patients.
      </Card>
      <Card icon="headset" title="World-class, US-based care">
        AI-first support resolves routine volume instantly; complex and clinical issues escalate to
        US-based human experts.
      </Card>
    </div>
  </Section>

  {/* 8.5 — Ownership / sovereignty */}
  <Section>
    <SectionHeader
      eyebrow="Ownership"
      title="You own it. We run the rails."
      lead="Purple Label is infrastructure, not a middleman. The brand, the patients, and the data are yours — run by your team, your agents, or ours, on a back end that already operates at national scale."
    />
    <div class="mt-12 grid gap-6 md:grid-cols-3">
      {
        ownership.map((o) => (
          <Card icon={o.icon} title={o.title}>
            {o.body}
          </Card>
        ))
      }
    </div>
    <p class="mt-8 max-w-prose text-body-lg text-ink-500">
      <span class="font-semibold text-ink-900"
        >You own the brand; licensed providers handle the medicine.</span
      > A board-certified network credentialed in all 50 states evaluates, prescribes, and manages clinical
      care — so you carry the brand, not the clinical license.
    </p>
  </Section>

  {/* 9 — Security & Developers teasers */}
  <Section surface="sunken" bordered>
    <div class="grid gap-6 md:grid-cols-2">
      <Card icon="shield-check" title="Built for PHI from day one" href="/security">
        A HIPAA-compliant data layer with end-to-end encryption, MFA, and granular access control —
        the controls behind cleared HIPAA, state pharmacy-board, and payment-processor reviews.
      </Card>
      <Card icon="code" title="Everything is programmable" href="/developers">
        Point your agents at our API, MCP servers, agent-ready docs, and prompt library and build
        the rest.
      </Card>
    </div>
  </Section>

  {
    /* 10 — Customer proof: removed per Gregg 2026-07-23 until real Purple Label customer
       stories exist. The Steenblik/Trucker's Body Shop quote (verbatim from WLMD's live
       site) lives in git history — restore it here when examples are ready. */
  }

  {/* 10.5 — FAQ */}
  <Section>
    <SectionHeader
      align="center"
      eyebrow="Questions, answered"
      title="The essentials, before you build."
    />
    <FAQ class="mt-12" items={homeFaqs} />
  </Section>

  {/* 11 — Closing CTA */}
  <Section surface="surface" bordered>
    <SectionHeader
      align="center"
      eyebrow="Get started"
      title="Launch a branded telehealth business — on infrastructure that's already proven."
    />
    <div class="mt-8 flex flex-wrap items-center justify-center gap-4">
      <Button variant="primary" href="/contact">Book a consult</Button>
      <Button variant="secondary" href="/pricing">See pricing</Button>
    </div>
  </Section>
</PageLayout>

<script>
  /*
   * Live "prescriptions filled" ticker. Seeds from a time-anchored base (so it keeps
   * climbing across reloads/sessions), then ticks up in irregular bursts for a live feel:
   * variable interval (~0.9–3.2s) and variable step (+1/+2/+3), far faster than a flat rate.
   */
  const ANCHOR_MS = Date.UTC(2026, 5, 28);
  const ANCHOR_COUNT = 728_487;
  const AVG_PER_DAY = 60_000; // seeds the base across sessions (≈ the live tick average)

  const counter = document.getElementById('rx-counter');
  if (counter) {
    const fmt = new Intl.NumberFormat('en-US');
    let count = ANCHOR_COUNT + Math.floor(((Date.now() - ANCHOR_MS) / 86_400_000) * AVG_PER_DAY);
    counter.textContent = fmt.format(count);

    const step = () => {
      const r = Math.random();
      return r > 0.9 ? 3 : r > 0.65 ? 2 : 1;
    };
    const schedule = () => {
      setTimeout(
        () => {
          count += step();
          counter.textContent = fmt.format(count);
          schedule();
        },
        900 + Math.random() * 2300,
      );
    };
    schedule();
  }
</script>
